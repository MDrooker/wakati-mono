services:
  wakati-api:
    build:
      context: .
      args:
        NPM_TOKEN: ${NPM_TOKEN}
    platform: linux/aarch64,linux/arm64
    env_file:
      - .env
    ports:
      - '8080:8080'
    environment:
      - NODE_ENV=production

